# Globale TODO-Liste

## Claude Config Backup (GitHub)

- [ ] GitHub-Account anlegen (falls noch nicht vorhanden)
- [ ] Privates Repo `claude-config` auf GitHub erstellen
- [ ] Remote setzen und pushen:
  ```bash
  cd ~/.claude
  git remote add origin git@github.com:Maddi/claude-config.git
  git push -u origin master
  ```
- [ ] Bei Änderungen an CLAUDE.md / MEMORY.md / skills/ committen und pushen

## Skills

- [x] Skills (`~/.claude/skills/`) in Git versionieren
  - Erledigt: skills/ ist im ~/.claude-Repo enthalten

